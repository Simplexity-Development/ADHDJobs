package adhdmc.adhdjobs;

/*import com.jeff_media.customblockdata.CustomBlockData;
import org.bukkit.Bukkit;
import org.bukkit.Location;
import org.bukkit.NamespacedKey;
import org.bukkit.block.Block;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;
import org.bukkit.event.block.BlockPlaceEvent;
import org.bukkit.persistence.PersistentDataContainer;
import org.bukkit.persistence.PersistentDataType;

import java.io.IOException;

public class BlockPlaceListener implements Listener {



    NamespacedKey placedBlock = new NamespacedKey(ADHDJobs.instance, "placedBlock");

    byte f = 0;
    byte t = 1;
    @EventHandler
    public void placedBlock(BlockPlaceEvent event){

        Block block = event.getBlock();
        Location blockLocation = block.getLocation();
        String blockLocationString = blockLocation.toString();
        PersistentDataContainer chunkPDC = block.getChunk().getPersistentDataContainer();
        if (chunkPDC.has(placedBlock)){
            chunkPDC.set(placedBlock, PersistentDataType.);
        }


    }
}
*/